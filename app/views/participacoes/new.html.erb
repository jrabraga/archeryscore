<%= form_for @participacao, url: evento_participacoes_path(@evento), html: {} do |f| %>
  <%= render :partial => 'form', :locals => {:evento => @evento, :participacao => @participacao, :f => f} %>
<% end %>